# Option 1: Feature release (v0.1.0)
git commit --allow-empty -m "feat: initial release of fresh-normalize-beta CSS library"

# Option 2: Fix release (v0.0.1)
git commit --allow-empty -m "fix: initial working version"

# Option 3: If ready for stable release with breaking change (v1.0.0)
git commit --allow-empty -m "feat!: stable release

# BREAKING CHANGE: Initial stable release"
